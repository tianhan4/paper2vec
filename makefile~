CC = gcc
#CFLAGSW2V = -lm -O0 -pthread -march=native -Wall -ggdb
CFLAGSW2V = -lm -pthread -O3 -march=native -Wall -funroll-loops -Wno-unused-result

all: word2vec

word2vec : word2vec.c
	$(CC) word2vec.c -o word2vec $(CFLAGSW2V)
clean:
	rm -rf word2vec
